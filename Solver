public class Solver {

	public ArrayList clustering(Graph g) {
		ArrayList nodes = g.getGraph();
		for (int i = 0; i <= g.size; i ++ ) {

		}
	}

	public ArrayList findRoute(Graph g) {

	}

	public ArrayList findClosestCenter(ArrayList nodes, ArrayList centers) {
		
		ArrayList<ArrayList> whichCenter = new ArrayList<ArrayList>();

		for (int n = 0; n < centers.size(); n ++ ) {
			ArrayList<Node> temp = new ArrayList<Node>();
			Node nodeToAdd = nodes.get(0);
			for (int i = 0; i < nodes.size(); i ++ ) {
				if (nodes.get(i).distanceToNode(centers.get(n)) > nodeToAdd) {
					nodeToAdd = nodes.get(i);
					temp.add(centToAdd);
				}		
			}

			whichCenter.add(temp);
		}
	}

}
